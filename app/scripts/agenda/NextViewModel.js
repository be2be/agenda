define(['knockout'], function (ko) {
    'use strict';

    var FolderViewModel = function () {
        var self = this;

        self.template = 'next-template';
    };

    return FolderViewModel;
});
